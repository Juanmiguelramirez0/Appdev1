export default function UITreeConcept() {
  return (
    <div>
      <h2>Done with why understanding your UI as trees is useful</h2>
      <p>
        Your React UI works like a tree — the App is the root, and components
        are its branches. This helps React update only the parts that change.
      </p>
    </div>
  );
}
