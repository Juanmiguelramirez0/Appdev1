export default function MarkupExample() {
  return (
    <div>
      <h2>JSX allows us to write HTML-like syntax in JavaScript</h2>
      <p>This makes UI code more expressive and readable.</p>
    </div>
  );
}
